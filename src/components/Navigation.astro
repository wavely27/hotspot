---
import { cn } from "../lib/utils";

interface Link {
  href: string;
  label: string;
  icon: string;
}

const links: Link[] = [
  { href: '/daily/trending', label: '热点', icon: '🔥' },
  { href: '/daily/tech', label: '技术', icon: '💻' },
  { href: '/daily/business', label: '商业', icon: '💼' },
  { href: '/github', label: 'GitHub', icon: '⭐' },
  { href: '/huggingface', label: 'HuggingFace', icon: '🤗' },
];

const baseUrl = import.meta.env.BASE_URL.replace(/\/$/, '');
const currentPath = Astro.url.pathname;
---

<nav class="flex items-center gap-1">
  {links.map(link => {
    const fullHref = `${baseUrl}${link.href}`;
    const isActive = currentPath.startsWith(fullHref);
    
    return (
      <a 
        href={fullHref}
        class={cn(
          "inline-flex items-center gap-2 px-3 py-2 rounded-md text-sm font-medium transition-colors",
          isActive 
            ? "bg-slate-100 text-brand-primary" 
            : "text-slate-600 hover:text-slate-900 hover:bg-slate-50"
        )}
      >
        <span>{link.icon}</span>
        <span>{link.label}</span>
      </a>
    )
  })}
</nav>
